<?php

echo
'Operador el cual tiene tres operandos o argumentos. Usando este operador podemos pasar de tener varias líneas' . '<br>' .
    'de código a tener una sola. Este operador sirve para expresar una sentencia condicional es decir un if. Y en' . '<br>' .
    'la mayoría de lenguajes de programación, es el único que existe como operador ternario. Sólo tenemos un operador' . '<br>' .
    'ternario. Así que podemos llamarlo “el operador ternario”' . '<br>' . '<hr>';

echo
'(5 > 7) ? $total=10 * 7 : $total=2 * 4;' . '<br>' .
    'echo $total;' . '<br>' . '<hr>';

(5 > 7) ? $total = 10 * 7 : $total = 2 * 4;
echo $total;

echo '<hr>';

echo
'(8 > 7) ? $total=10 * 7 : $total=2 * 4;' . '<br>' .
    'echo $total;' . '<br>' . '<hr>';

(8 > 7) ? $total = 10 * 7 : $total = 2 * 4;
echo $total;
